# 開発ルール

1. `main`へ直接大きな変更を入れず、担当ごとにブランチを作る。
2. 機能追加時は対応する`tests/`も更新する。
3. 解析結果のデータ型を変える場合は、先に`domain/models.py`の変更を共有する。
4. UI担当は原則として`parsers/`や`services/`の内部関数を直接呼ばない。
5. 外部サービスのAPIキーはGitHubへ保存しない。
6. Pull Requestには「変更内容」「確認した入力例」「残る課題」を書く。
